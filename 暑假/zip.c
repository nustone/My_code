#include<stdio.h>
#include<string.h>
void zip(char *str){
    int len=strlen(str);
    for (int i = 0; i < len; i++)
    {
        if (str[i+1==str[i]])
        {
            
        }
        
    }
    
}
int main(){
    char str[20]="aaabbbccc";
    zip(str);
}